<%= form_for @post do |f| %>
<% if @post.errors %>
<ul>
    <% @post.errors.full_messages.each do |m| %>
    <li><%= m %></li>
    <% end %>
</ul>
    <% end %>

<%= f.text_field :body, placeholder: "Your text..." %>
<%= f.submit :submit %>
<%end%>